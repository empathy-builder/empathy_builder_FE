import React from 'react'

class DataSearch extends React.Component {
    render(){
        return(
            <>
                
            </>
        )
    }
}

export default DataSearch

//needs two params... column, filter... will return all the data with the desired information
//the action call should push to 